module(name = "hidapi-cpp", version = "0.0.1")

bazel_dep(name = "rules_cc", version = "0.2.16")

non_module_deps = use_extension("//:extension.bzl", "non_module_deps")
use_repo(
    non_module_deps,
    "hidapi",
)

include("//:toolchain.MODULE.bazel")
